buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2' // Cập nhật nếu cần
        classpath 'com.google.gms:google-services:4.3.15' // Thêm dòng này
    }
}
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}